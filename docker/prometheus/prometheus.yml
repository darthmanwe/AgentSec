# Placeholder scrape config so the observability profile starts. Real targets and
# recording rules arrive with AS-041.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
