# repo-a

A small fixture repository used by the AgentSec test suite.

Contains one deliberately vulnerable file (`src/main.py`) and one clean control
(`src/safe.py`). The real corpus, with hidden ground truth, is built in AS-034.
